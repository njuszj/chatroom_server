apiVersion: apps/v1
kind: Deployment
metadata: 
  name: server-deployment
spec:
  selector:
    matchLabels:
      app: server-deployment
  replicas: 1
  template:
    metadata:
      labels: 
        app: server-deployment
    spec:
      containers:
      - name: server-container
        image: "server_app:lastest"
        ports:
        - name: listen-port
          containerPort: 9999
          hostPort: 9999
        - name: ssh-port
          containerPort: 22
          hostPort: 15000
